# bash aliases

# Git
alias gs="git status"
alias gl="git log --oneline"
alias gd="git diff"
alias gr="git commit --amend --no-edit && git rebase --continue"

# Docker
alias di="docker images"
alias dp="docker ps"
alias dc="docker container ls"
alias dv="docker volume ls"
alias dn="docker network ls"
